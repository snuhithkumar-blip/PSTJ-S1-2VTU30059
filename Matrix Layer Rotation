import java.io.*;
import java.math.*;
import java.security.*;
import java.text.*;
import java.util.*;
import java.util.concurrent.*;
import java.util.function.*;
import java.util.regex.*;
import java.util.stream.*;
import static java.util.stream.Collectors.joining;
import static java.util.stream.Collectors.toList;

class Result {

    /*
     * Complete the 'matrixRotation' function below.
     *
     * The function accepts following parameters:
     *  1. 2D_INTEGER_ARRAY matrix
     *  2. INTEGER r
     */

    
public static void matrixRotation(List<List<Integer>> matrix, int r) {

    int m = matrix.size();
    int n = matrix.get(0).size();

    int[][] result = new int[m][n];

    // Copy original matrix
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            result[i][j] = matrix.get(i).get(j);
        }
    }

    int layers = Math.min(m, n) / 2;

    for (int layer = 0; layer < layers; layer++) {

        int top = layer;
        int bottom = m - 1 - layer;
        int left = layer;
        int right = n - 1 - layer;

        // Store layer elements
        List<Integer> elements = new ArrayList<>();

        // Top row
        for (int j = left; j <= right; j++) {
            elements.add(result[top][j]);
        }

        // Right column
        for (int i = top + 1; i <= bottom; i++) {
            elements.add(result[i][right]);
        }

        // Bottom row
        for (int j = right - 1; j >= left; j--) {
            elements.add(result[bottom][j]);
        }

        // Left column
        for (int i = bottom - 1; i > top; i--) {
            elements.add(result[i][left]);
        }

        int len = elements.size();
        int shift = r % len;

        int index = 0;

        // Top row
        for (int j = left; j <= right; j++) {
            result[top][j] = elements.get((index + shift) % len);
            index++;
        }

        // Right column
        for (int i = top + 1; i <= bottom; i++) {
            result[i][right] = elements.get((index + shift) % len);
            index++;
        }

        // Bottom row
        for (int j = right - 1; j >= left; j--) {
            result[bottom][j] = elements.get((index + shift) % len);
            index++;
        }

        // Left column
        for (int i = bottom - 1; i > top; i--) {
            result[i][left] = elements.get((index + shift) % len);
            index++;
        }
    }

    // Print result
    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            System.out.print(result[i][j]);

            if (j < n - 1) {
                System.out.print(" ");
            }
        }
        System.out.println();
    }
}

}

public class Solution {
    public static void main(String[] args) throws IOException {
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));

        String[] firstMultipleInput = bufferedReader.readLine().replaceAll("\\s+$", "").split(" ");

        int m = Integer.parseInt(firstMultipleInput[0]);

        int n = Integer.parseInt(firstMultipleInput[1]);

        int r = Integer.parseInt(firstMultipleInput[2]);

        List<List<Integer>> matrix = new ArrayList<>();

        IntStream.range(0, m).forEach(i -> {
            try {
                matrix.add(
                    Stream.of(bufferedReader.readLine().replaceAll("\\s+$", "").split(" "))
                        .map(Integer::parseInt)
                        .collect(toList())
                );
            } catch (IOException ex) {
                throw new RuntimeException(ex);
            }
        });

        Result.matrixRotation(matrix, r);

        bufferedReader.close();
    }
}
